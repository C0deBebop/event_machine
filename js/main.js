const age = document.querySelector("#age").value;
const maritalStatus = document.querySelector("#marital_status");
const hasChildren = document.querySelector("button[value='yes']");
const hasNoChildren = document.querySelector("button[value='no']");
const profilePhoto = document.querySelector("input[type='file']");*/

hasChildren.addEventListener('click', (e) => {
    e.preventDefault()
})
